---
title: Custom Plugins
---

# Custom Plugins

Add a package under `odock-server/internal/plugins/<name>`, implement config parsing and a plugin factory, register it, then add it to `configs/plugins.json` under the phases where it should run.

---
title: User Management
description: Organisations, users, teams, roles, invitations, and user-level operational views.
---

# User Management

User management is handled in `odock-ui`. It controls who can configure the gateway and who owns runtime credentials. Runtime calls to `odock-server` use virtual API keys rather than UI login sessions.
